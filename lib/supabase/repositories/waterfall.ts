import type { Waterfall } from "../types";

export async function getWaterfalls(): Promise<Waterfall[]> {
  return [];
}

export async function getWaterfallBySlug(slug: string): Promise<Waterfall | null> {
  return null;
}
